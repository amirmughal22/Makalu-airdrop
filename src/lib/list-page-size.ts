/** Recipients table, batch job results on execution page, and expanded job transaction rows (per job). */
export const LIST_PAGE_SIZE = 20;

/** Jobs list on dashboard history (`/api/airdrop/history` page size). */
export const HISTORY_JOBS_PAGE_SIZE = 20;

/** Distributor wallet lists (execution + dashboard + fund recipients). */
export const DISTRIBUTOR_WALLETS_PAGE_SIZE = 20;
